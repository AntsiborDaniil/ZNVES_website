upstream znves_next {
    server app:3000;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;
    server_name znves.ru www.znves.ru;

    # ACME HTTP-01 (certbot)
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type "text/plain";
    }

    # Пока SSL не выпущен — проксируем HTTP.
    # После certbot раскомментируйте блок HTTPS ниже и включите redirect.
    location / {
        proxy_pass http://znves_next;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "";
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }
}

# --- HTTPS (раскомментировать после получения сертификатов) ---
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name znves.ru www.znves.ru;
#
#     ssl_certificate     /etc/letsencrypt/live/znves.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/znves.ru/privkey.pem;
#     ssl_session_timeout 1d;
#     ssl_session_cache shared:SSL:10m;
#     ssl_protocols TLSv1.2 TLSv1.3;
#
#     # Редирект www → apex (или наоборот — по желанию)
#     # if ($host = www.znves.ru) {
#     #     return 301 https://znves.ru$request_uri;
#     # }
#
#     location / {
#         proxy_pass http://znves_next;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "";
#         proxy_read_timeout 60s;
#         proxy_send_timeout 60s;
#     }
# }
#
# server {
#     listen 80;
#     listen [::]:80;
#     server_name znves.ru www.znves.ru;
#     location ^~ /.well-known/acme-challenge/ {
#         root /var/www/certbot;
#     }
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }
